{
    "name": "4ever",
    "version": "1.0.0",
    "scripts": {
        "install-all": "pushd 4everfrontend && npm install && popd && pushd 4everbackend && npm install && npm install -g truffle && popd",
        "client": "pushd 4everfrontend && npm run dev && popd",
        "server": "pushd 4everbackend && npm start && popd",
        "truffle": "pushd 4everbackend && pushd Truffle && truffle migrate && node DotEnv.js && popd && popd"
    }
}